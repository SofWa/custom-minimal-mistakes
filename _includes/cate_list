{% include group-by-array collection=site.posts field="categories" %}

<h3>Category</h3>

{% for category in group_names %}
  {% assign posts = group_items[forloop.index0] %}
  <p>{{ category }}({{ posts | size }})</p>
{% endfor %}
