{% include base_path %}

<nav class="nav__list">
  <ul>
    <li>
      <span class="nav__sub-title">Year</span>
      <ul
        {% for post in site.posts %}
          {% capture year %}{{ post.date | date: '%Y' }}{% endcapture %}
          <li><a href="#{{ year }}">{{ year }} ({{ post | size }})</a></li>
        {% endfor %}
      </ul>
    </li>
  </ul>
</nav>
